[project]
name = "edgehacker"
version = "0.1.0"
description = "edgehacker"
requires-python = ">=3.8, <4.0"

[tool.nonebot]
adapters = [
    { name = "OneBot V11", module_name = "nonebot.adapters.onebot.v11" }
]
plugin_dirs = ["plugins"]
